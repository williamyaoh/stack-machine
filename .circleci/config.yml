version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2024.01
    steps:
      - checkout
      - run:
          name: Run make all
          command: make all

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
